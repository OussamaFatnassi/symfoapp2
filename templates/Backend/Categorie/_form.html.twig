{{ form_start(form, {attr: {class: 'form card p-3 w-50 mx-auto'}}) }}

<div class="row align-items-center">
	<div class="col-md-6">{{ form_row(form.title) }}</div>
	<div class="col-md-6">{{ form_row(form.enable) }}</div>
</div>

<div class="text-center">
	<button type="submit" class="btn btn-primary">{{ buttonLabel|default('Save') }}</button>
</div>

{{ form_end(form) }}
